data nat :=
  Zero
| Suc nat.

copy my_nat := nat
  partial_quotient (fun x y. (x = Suc Zero || x = Suc (Suc Zero)) && (y = Suc Zero || y = Suc (Suc Zero)))
  abstract my_nat_of_nat
  concrete nat_of_my_nat.

goal exists (x : my_nat) (y : my_nat). ~ (x = y).
