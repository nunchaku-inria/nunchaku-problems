data digit :=
  Zero
| One
| Two.

copy my_digit := digit
  subset (fun x. x = One || x = Two)
  abstract my_digit_of_digit
  concrete digit_of_my_digit.

goal exists (x : my_digit) (y : my_digit) (z : my_digit). ~ (x = y) && ~ (x = z) && ~ (y = z).
