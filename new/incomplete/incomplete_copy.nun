data nat :=
  Zero
| Suc nat.

copy my_nat := nat
  subset (fun x. x = Suc Zero || x = Suc (Suc Zero))
  abstract my_nat_of_nat
  concrete nat_of_my_nat.

goal forall (x : my_nat). nat_of_my_nat x = Zero || ~ (nat_of_my_nat x = Zero).
