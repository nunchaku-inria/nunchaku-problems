# outcome: "CVC4 threw an "unexpected" exception. libc++abi.dylib: unexpected_handler unexpectedly returned"
# expect: SAT (or at least an error that we are leaving the FO fragment)

val a : type.
val b : type.

val f : a -> b.
val g : a -> b.

goal (f = g) => false.
