# outcome: model with "if ... then b_1 else b_1" and if ... then b_0 else b_0"
# expected: SAT model without trivial ifs (irrespective of whatever junk you get from backend)

val a : type.
val b : type.

val f : a -> b.
val g : a -> b.
val x : a.

goal ~ ((exists (x1 : a). f x1 = g x1) => (f x = g x)).
