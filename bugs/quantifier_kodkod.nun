
# outcome: sat (with Kodkod)
# outcome: unsat (with SMBC v4)
# expected: unsat

# There's definitely a bug in the Kodkod encoding.

data x := A | B | C | D | E | F | G | H | I | J | K | L.

goal forall (a : x) (b : x). a = b.
