# outcome: sat with "LCons $a_0 (mu (v_0/56:llist). LCons $a_0 v_0/56)"
# expected: sat with "mu (v_0/56:llist). LCons $a_0 v_0/56"

# The cycle shouldn't be repeated.

val a : type.

codata llist :=
  LNil
| LCons a llist.

val xs : llist.

goal exists x. xs = LCons x xs.
