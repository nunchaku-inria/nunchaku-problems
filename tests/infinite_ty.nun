# expect: sat

# small example with an infinite type and its approximation

val u : type [infinite].

val alpha_u : type [approx_of u].

val inject : alpha_u -> u.

val f : u -> u.

axiom forall x. f (inject x) = inject x.

goal exists y. f y = y.


