
# Test for copy
# expect: SAT

data pair a b := Pair a b.

copy pair1 a := pair a a
  abstract pair1_of_pair
  concretize pair_of_pair1.

val iota : type.

goal forall (p:pair1 iota).
     exists (x:iota).
     pair_of_pair1 p = Pair x x.


