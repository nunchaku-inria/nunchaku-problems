
# test partial application and HO
# expect: sat

val a : type.
val b : type.
val c : type.

val comp : (a -> b) -> (b -> c) -> a -> c.

axiom forall x f g. comp f g x = g (f x).

# so non-meaningful!
val pseudo_pred : pi a. a -> prop.

axiom pseudo_pred comp.
axiom forall f g. pseudo_pred (comp f g).


