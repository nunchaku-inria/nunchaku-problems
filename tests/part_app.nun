
# test partial application and HO
# expect: unknown

val a : type.
val b : type.
val c : type.

val comp : a -> (a -> b) -> (b -> c) -> c.

axiom forall x f g. comp x f g = g (f x).

# so non-meaningful!
val pseudo_pred : pi a. a -> prop.

axiom pseudo_pred comp.
axiom forall x f. pseudo_pred (comp x f).


